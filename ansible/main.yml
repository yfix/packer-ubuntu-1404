---
- hosts: all
  sudo: yes
  gather_facts: yes

  roles:
    - packer-debian

  tasks:
    - apt: "name={{ item }} state=installed"
      with_items:
        - git
        - wget
        - curl
        - vim
        - mc
        - htop
        - iotop
        - nfs-common
